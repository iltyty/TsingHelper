<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_marginTop="@dimen/message_item_margin_top"
    android:layout_marginBottom="@dimen/message_item_margin_bottom"
    android:background="#00000000">

    <ImageView
        android:id="@+id/message_item_icon"
        android:layout_width="@dimen/message_item_icon_size"
        android:layout_height="@dimen/message_item_icon_size"
        android:layout_centerVertical="true"
        android:scaleType="fitCenter" />

    <TextView
        android:id="@+id/message_item_content"
        android:layout_width="wrap_content"
        android:layout_height="match_parent"
        android:layout_centerVertical="true"
        android:layout_marginStart="@dimen/message_item_text_margin_start"
        android:layout_marginLeft="@dimen/message_item_text_margin_start"
        android:layout_toEndOf="@id/message_item_icon"
        android:layout_toRightOf="@id/message_item_icon"
        android:gravity="center_vertical"
        android:textSize="@dimen/state_item_text_size" />

    <TextView
        android:id="@+id/message_item_time"
        android:layout_width="wrap_content"
        android:layout_height="match_parent"
        android:layout_alignParentEnd="true"
        android:layout_alignParentRight="true"
        android:gravity="center_vertical" />

</RelativeLayout>
